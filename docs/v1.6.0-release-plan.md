# v1.6.0 — Release Definition & Plan

> Created 2026-09-26. Defines the content of the next minor release.
> **Status: content COMPLETE on `main`, NOT yet released.** All features below
> are committed, verified, and production-tested; the release itself (version
> bump, tag, npm publish) is pending the release gate described at the end.
>
> NOTE on the existing tag `v1.6.0-pre-stream-orchestrator` (797f958): that tag
> is a historical START marker from an earlier numbering plan in which the
> stream-orchestrator refactor was going to be v1.6.0. The refactor shipped in
> the v1.5.x line instead (branch `refactor/stream-orchestrator` is fully
> merged; since v1.5.4 the file saw only 27 insertions). This document defines
> the ACTUAL v1.6.0: everything accumulated since v1.5.4.

## Scope: all commits on `main` since tag `v1.5.4`

**29 commits, 72 files changed, +7112 / −318 lines.**
Current HEAD: `db6ac87`. Bundle `dist/index.js`: 534,502 bytes (2026-09-20 22:17).

---

## 1) Use-Case Model Groups: `bulk_reader` / `code_writer` (merged PR #1)

Cost-based routing gets use-case-specific groups with a context-length floor,
addressable as `bulk_reader/bulk_reader` and `code_writer/code_writer` from
subagent workflows.

- `min_context_length` field on the `Group` interface (strict gate: an unknown
  context window fails the gate — registry-first via `lookupContextWindow(ref)`,
  `src/metrics.ts:420`).
- Applied in BOTH paths: live (`applyGroupFilters`, `src/routing.ts:208-215`)
  and persist (`filterModelsForGroup`, `src/dynamic-config.ts:175-182`).
- Groups declared in `router-config.json`:
  - `bulk_reader`: 64k context floor, tiered, `local_first`, max_cost 2.0 —
    for I/O-heavy subtasks (read + summarize several files).
  - `code_writer`: 32k context floor — spec + one reference file
    (boilerplate generation).
- README documents both groups and the filter.

Commits: `f172ee4`, `9d41a4e`, `8579b8f`, `4bca8f6`, `9cad38b`, `6d9a3dd`,
`48f9cf3`, `186c538`, `a2a3893`, `96c97c4`, merge `c33d8b5`.
Tests: context-window-registry.test.ts, dynamic-config min_context_length tests.

## 2) Enforced Delegation (ADR-0007, Revision 2026-09-20 a+b)

The Spotify Portal/shunt architecture: the expensive model pays for answers,
never for bulk content. Three delivered pieces:

- **Tool-result shrinker** (`src/delegation.ts`): oversized `read` AND `bash`
  results are summarized by the delegation group (default `bulk_reader`) in the
  `tool_result` hook, fail-open everywhere. Threshold-gated (`min_chars` 3500,
  shunt-aligned). Targeted reads and piped/grep'd bash pass through untouched.
  Router narration (`> [router]` lines) is stripped from sub-call output.
- **Read pre-block** (`src/bulk-read.ts`, `checkReadBlock`): a full-file read
  (no offset/limit) of a file above `block_lines` (default 350) is blocked in
  the `tool_call` hook BEFORE execution; the reason redirects to `bulk_read` or
  a targeted read. `block_lines: 0` disables the block, the shrinker keeps
  working. Deliberate divergence from shunt: bash is NOT pre-blocked.
- **`bulk_read` tool** (`executeBulkRead`): question-based multi-file reading —
  files are read from disk, capped, sent to the delegation group; only the
  concise answer returns to the orchestrator.

Commits: `7240d01` (ADR feasibility + Spotify reference), `8e69304`, `44c6a10`,
`3660696`, `3a29be7`.
Tests: delegation.test.ts, bulk-read.test.ts (54/54).
Production telemetry (2026-09-20): 149 successful shrinks, 0 fail-opens,
0 narration-only results, 0 missing-group pass-throughs; spike verified nested
`usage` accounting (12,081 tokens, $0.00048).
ADR: `docs/adr/0007-task-decomposition-and-delegation.md` — revisions (a) and
(b) accepted; task-planner decomposition inside `streamSimple` remains
rejected (Pi subagent system's domain).

## 3) Ghost-Purge, HINT normalization, and slug matching

- **Shadowed alias-provider ghost purge** (`5ff46e6`): stale scan-cache entries
  (`mistral-zai/*` ghosts) and registry-missing refs never reach groups/configs;
  `redundantAliasProviders` + `pruneRedundantCacheEntries` (`src/provider-shadow.ts`).
- **HINT/MHINT** (same commit): `HINT: zai-glm-5.3` / `zai-glm-5_3` resolve to
  `mistral/zai-glm-5-3`; MHINT reserved for router narration.
- **`-latest` aliases and dated snapshots** (`263b2a6`, design `090f21c`):
  resolve to the canonical versioned model; dedup by matched slug in live,
  display, AND persist paths (TAG_SUFFIXES / VENDOR_PREFIXES machinery).
- **Dedup fixes**: GDPval duplicate-spelling slugs no longer merge different
  models (`481125d` — `glm-5-3`/`glm-53` are a true dup, `glm-5-2` vs `glm-52`
  are different models); persist path collapses slug clusters before the
  per-group cost filter (`55145d8`).
- **Classifier narration hygiene** (`80f60ab`): router narration stripped from
  ALL classifier-input paths.

Tests: mistral-zai-ghost-purge.test.ts, provider-shadow.test.ts,
model-matcher-batched.test.ts, slug-matcher-latest-dates.test.ts,
slug-canon-dedup.test.ts, detect-hint-synonyms.test.ts,
hint-normalization.test.ts.

## 4) Cost resolution: registry-first (effCost fix)

- `getM()` previously zeroed `cost_per_m` for subscription providers BEFORE
  consulting the registry — Mistral subscriptions with real prices ($1.4) read
  as "free" and landed in `max_cost:0` groups.
- `resolveCostPerM(ref)` is the authoritative chain: registry → local →
  subscription → cache → `:free` → `unknown`. Registry BEFORE subscription
  zeroing; SUB_DISCOUNT (0.5) applies only to explicitly subscription-billed
  providers (1.4 → 0.7).
- Healing in `getM()`'s early-return path fires ONLY for placeholders
  (`0` / `'unknown'`) — explicit user prices are never overwritten.
- Commits: `78bb3be` (incl. `bb89756`, `e2bc251` mock-realism prerequisites).
- Tests: metrics-cost-heal.test.ts (11 tests, rewritten non-vacuously).

## 5) Display footer for `/router status`

- `getTopModels` now returns `{ models, total }`; status renders
  `│ … +N weitere (sortiert nach [method])` per group so expensive models
  beyond the top-5 are visible as "present but ranked lower".
- Commit: `78bb3be`. Tests: get-top-models-total.test.ts.
- Verified visible in production by the owner (2026-09-26).

## 6) Test suite consolidation (audit-driven)

- Data-driven audit (subagent-driven, `docs/plans/2026-09-20-consolidate-tests.md`):
  age criterion (>6 months) matched ZERO files; the two originally suspected
  files never existed; all four examined candidates test LIVE features →
  no `.skip` deletions; timeout tuning rejected (slowest tests wait on REAL
  rate-limit cooldown windows).
- Executed merge: 5 exact-duplicate lookupGdp assertions removed from
  `refactor-golden-master.test.ts` (living counterparts pinned in
  `metrics-selfheal.test.ts`; unique coverage — wildcard match, pure
  token-set fallback, builtin non-shadowing — kept). Drift risk fixed
  (1506 vs 1506.11).
- Result: 806 → **801 tests** (798 passed / 3 skipped), 11.7s wall time.
- Evidence: `docs/plans/candidates_consolidation.md`,
  `docs/plans/redundancy-analysis.md` (incl. §4 flaky observation —
  load-correlated, unidentified; watch CI logs).
- Commits: `0bf5019`, docs translation `db6ac87` (CHANGES.md → English).

## 7) Process changes

- Roborev pre-release review gate replaced by the pi-superpowers
  `requesting-code-review` skill (`bd438c1`): reviewer subagent with fresh
  context, filled-in code-reviewer template; no Critical/Important findings
  may remain open.

---

## Verification status (at HEAD `db6ac87`)

| Check | Status |
|---|---|
| `npx tsc --noEmit` | ✅ clean |
| `npx vitest run` | ✅ 801 tests: 798 passed / 3 skipped |
| CI (run 35536011231, commit `0bf5019`) | ✅ green |
| Bundle | ✅ dist/index.js 534,502 bytes, built 2026-09-20 22:17 |
| Pi 0.87.1 compatibility | ✅ no API breakage (public registry APIs only) |
| Production telemetry | ✅ delegation shrinker healthy (149 shrinks / 0 failures) |
| Known open item | ⚠️ one intermittent, load-correlated flake (~3/17 runs early, 12× green after; unidentified — capture CI logs on first red run) |

## Release gate (in order — none of this is done yet)

Per `AGENTS.md` rule 1 and the `requesting-code-review` skill:

1. **Code review must be clean** — dispatch the reviewer subagent (fresh
   context) with the filled-in code-reviewer template:
   - `{WHAT_WAS_IMPLEMENTED}`: this document's §1-§7.
   - `{PLAN_OR_REQUIREMENTS}`: ADR-0007 (+revisions), use-case model groups
     plan, consolidation plan.
   - `{BASE_SHA}`: `v1.5.4` — `{HEAD_SHA}`: `db6ac87`.
   - No Critical or Important findings may remain open.
2. **Version bump** — `package.json` 1.5.4 → 1.6.0, update CHANGES.md header.
3. **Ask the owner** — explicit, release-specific approval naming "release 1.6.0".
   A clean review is a prerequisite, NOT approval.
4. **Only then**: tag `v1.6.0`, GitHub release, npm publish.
