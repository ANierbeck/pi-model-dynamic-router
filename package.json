{
  "name": "@anierbeck/pi-model-dynamic-router",
  "version": "1.3.1",
  "description": "Pi extension that routes model groups to concrete models. Balances intelligence, cost, and availability.",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "main": "dist/index.js",
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc && esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --external:@earendil-works/pi-ai --external:@earendil-works/pi-coding-agent --external:@earendil-works/pi-tui --external:@sinclair/typebox && cp router-defaults.yaml router-config.json model-map.yaml dist/",
    "prepack": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "TEST_INTEGRATION=true vitest run test/classifier.test.ts",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "if [ -z \"$(git tag --points-at HEAD 2>/dev/null)\" ]; then echo 'No git tag found at current commit, skipping publish'; exit 1; fi"
  },
  "keywords": [
    "pi",
    "pi-extension",
    "pi-package",
    "model-router",
    "llm",
    "ai"
  ],
  "author": "ANierbeck",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ANierbeck/pi-model-dynamic-router.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist/",
    "skills/",
    "PI.md",
    "README.md"
  ],
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@sinclair/typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.83.0",
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@earendil-works/pi-tui": "^0.83.0",
    "@eslint/js": "^10.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.2",
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.61.0",
    "@vitest/coverage-v8": "^3.2.6",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "execa": "^9.6.1",
    "prettier": "^3.8.4",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6",
    "yaml": "^2.0.0"
  }
}